# Review — Bytecode — 2026-06-12

## Retrieval
User described bytecode as "code that can run on any VM" and correctly placed it in the compiler → bytecode → VM chain.

## Evaluation
Correct relationships but missed the definition: bytecode is an intermediate representation — lower than source, higher than machine code. Portable across VMs, structured enough for efficient execution or JIT compilation.

## Verdict
Kept at 3-day interval. Correct but thin.
