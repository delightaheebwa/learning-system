# Review — man & Documentation — 2026-08-19

**Track:** SWE (Shell & Terminal)
**Question Type:** discriminative
**Interval:** 7d (held — needs more work)

**Question:** Offline and a command stumps you; you want quick practical examples, not a full manual page. Which tools do you reach for, how do their outputs differ, and why is `--help` often enough before `man`?

**Answer (user):** Reach for `tldr` — it gives quick usage examples for each command. `--help` is enough when you just want to know the different things a command can do (plain commands, no examples).

**Assessment:** ⚠️ Needs More Work (partial). Correct on `tldr` (practical examples) and `--help` (quick flag list). Missed `man <program>` — the authoritative *manual page* and the primary layer of the toolset. The 20% insight is the 4-layer ladder: `man` (authoritative reference) → `--help` (quick inline flag list) → `tldr` (worked examples) → LLM (conversational). Had the key tool but not the full picture; `man` was the missing core.

**Next Review:** 2026-08-26 (7d, held)
