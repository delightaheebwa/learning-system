# Review — What is the Shell — 2026-08-08

**Track:** SWE (Shell & Terminal)
**Question Type:** definitional
**Interval:** 7d → 7d (partial)

**Question:** In one sentence: what is the shell, and what does the `$` in `missing:~$` tell you?

**Answer:** The shell is a textual interface to run programs and combine them via pipes/redirects. `~` = home directory, `$` = normal user (vs `#` = root).

**Assessment:** Partial — shell definition correct (accepted commands, produced output). But conflated the prompt: `~` is home, `$` is the normal-user marker, not home.

**Next Review:** 2026-08-15 (7d)
