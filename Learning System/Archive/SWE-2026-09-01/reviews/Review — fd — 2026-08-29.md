# Review — fd — 2026-08-29

- Track: SWE (Shell & Terminal, MIT Missing Semester)
- Type: memory
- Last Q Type: definitional → asked discriminative
- Grade: FAIL

## Question
You reach for `fd` instead of `find`. What default behavior of `fd` gives you focused results out of the box that `find` doesn't do — and what is the tradeoff?

## Expected (not leaked)
Respects `.gitignore` by default (skips ignored files) for repo-aware, focused results; tradeoff = less portable than `find` (not POSIX).

## Learner answer
Said fd skips hidden files by default while find includes them. True but minor; missed the `.gitignore` default and named no tradeoff.

## Error analysis
- error_type: structural
- self_attribution: recalled identity (find alternative, easier syntax) but missed the load-bearing `.gitignore`-respecting default; named the minor default (hidden files) instead.
- Root: conflates fd's two defaults; the repo-aware one is the 80% insight.

## Action
Mistake row updated (retry 2026-08-30). Next review 2026-08-30. Feynman explain-back still due (advisory).