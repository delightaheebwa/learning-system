# KNOWLEDGE BASE — Active Concepts

> Purpose: Active learning record for the current focus area. The assistant reads this at the start of learning sessions.
> Paused/archived concepts: `Learning System/Core/📦 Concept Archive.md` (searched on demand, not auto-loaded)
> Scripture memory: `Learning System/Core/📖 Scripture Memory.md`

## Metadata

- **Tracks:** SWE (Software Engineering Fundamentals) — Shell & Terminal
- **Learner:** Aheebwa Delight
- **Source:** MIT Missing Semester — https://missing.csail.mit.edu/2026/course-shell/
- **Previous Track:** AI Engineering (aie) — archived 2026-07-28
- **Last Updated:** 2026-08-28
- **Interleaving:** Active (shuffle + adjacency constraint + alternating question types)
- **System:** Open WebUI Learning System

---

## Live System Notes

- Use **"swe"** to trigger Software Engineering track reviews
- Use **"lesson"/"continue"** to run the next Stage-0 curriculum lesson (see `Learning System/CURRICULUM.md`; delegated to the `learning-teach` skill — probe → plan → teach, live fact-checking, no Mimo gate on teaching)
- Each trigger runs a separate review session limited to that track's due concepts (cap of 5 per session)
- `Sessions/` is the session history for the active learning system
- `Reviews/` stores spaced-repetition review notes
- `Concept Notes/` stores reusable atomic concept pages
- `Archive/` and `📦 Concept Archive.md` are reference-only
- This file is the source of truth for what is due and what is developing
- No more than 5 review concepts per session; overflow stays queued
- Previous tracks (aie, makemore) archived on 2026-07-28 — all reviews paused
- **Consolidation:** When a concept reaches `consolidated` status, it is moved to `Archive/Consolidated/[name].md` with a link to its Knowledge Wiki note and removed from this table.

---

## SWE Track — Shell & Terminal (MIT Missing Semester)

> Concepts from MIT Missing Semester Lecture 1: Course Overview + Introduction to the Shell
> Status values: `not_started` → `developing` → `pending_mastery` → `mastered` → `consolidated`
> Type-aware intervals: memory [0d,1d,3d,7d,14d,30d,60d] · concept [3d,7d,14d,30d] · procedure [3d,7d,14d] · design [14d,28d] (interval_index state machine: +1 on pass, +2 on 2 consecutive passes, -1 on fail; see Core/Attempts.json)

| Concept | Type | Status | Prerequisites | Last Reviewed | Next Review | Source | Last Q Type | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| What is the Shell | concept | developing | None | 2026-08-26 | 2026-09-09 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | The shell is a textual interface to run programs, combine them via pipes/redirects, and automate workflows. Terminal app → shell prompt (missing:~$), where $=normal user, #=root. Run any program by name. Reviewed 2026-08-21: MOSTLY PASS (discriminative) — decoded machine/~/normal-user correctly; MISFIRE: called `#` a "guest" — it is root/superuser (the opposite; max privileges). Prompt tells you how dangerous your next command could be. Held 7d. Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Navigation & Paths | procedure | developing | What is the Shell | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | cd (built-in, not a program), pwd, paths: absolute (start /) vs relative (from cwd). `.` = this dir, `..` = parent, `~` = home. Tab completion works. zoxide for faster cd. Reviewed 2026-08-19: built-in vs external distinction — `cd` must be a built-in (a child process can't propagate a cwd change back); `pwd` is also a built-in in bash; `ls` is external. Wiki: [[MIT Missing Semester — Shell]]. |
| man & Documentation | procedure | developing | What is the Shell | 2026-08-24 | 2026-09-07 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `man <program>` opens the manual page. `--help` is a quick flag for most commands. tldr shows practical examples. LLMs also work well for explaining commands. The 4-layer ladder: man (authoritative) → --help (quick flag list) → tldr (worked examples) → LLM (conversational). Reviewed 2026-08-19: held at 7d — had tldr/--help but missed man as the primary layer. Reviewed 2026-08-24: PASS (definitional) — man-first ladder rebuilt cleanly (authoritative docs → worked examples → quick flags); 1 more correct recall graduates the mistake. Advanced 7d → 14d. Wiki: [[MIT Missing Semester — Shell]]. |
| PATH & Program Discovery | concept | developing | What is the Shell | 2026-08-24 | 2026-08-27 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Shell consults `$PATH` env var — a list of directories searched in order when a command is typed. `which <program>` shows which file is executed. Bypass PATH by giving the full path. `ls /bin` shows available programs. Reviewed 2026-08-24: FAIL (discriminative) — nailed why ./a.out works (explicit path) but could not say where bare-name lookup searches ($PATH ordered directory walk; cwd deliberately absent). Distinction: name→PATH-search vs path→direct execution. Retry 2026-08-27. Wiki: [[MIT Missing Semester — Shell]]. |
| ls & File Listing | procedure | developing | Shell Navigation & Paths | 2026-08-15 | 2026-08-29 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `ls <path>` lists directory contents. `ls /bin` shows every program on PATH. `ls -l` for detailed listing (permissions, size, date). eza is a more human-friendly alternative. Wiki: [[MIT Missing Semester — Shell]]. |
| Basic File Tools (cat, sort, uniq, head, tail) | procedure | developing | What is the Shell | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `cat file` prints file contents. `sort file` sorts lines. `uniq file` removes consecutive duplicates. `head`/`tail` show first/last lines. bat is a cat alternative with syntax highlighting. `uniq` only collapses consecutive duplicates — true dedup/count needs `sort | uniq -c`. Advanced 7d → 14d 2026-08-19 (pass). Wiki: [[MIT Missing Semester — Shell]]. |
| bat | memory | developing | Basic File Tools | 2026-08-21 | 2026-09-04 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `bat` is a drop-in replacement for `cat` with syntax highlighting, line numbers, and paging/scrolling. Install from https://github.com/sharkdp/bat. Reviewed 2026-08-21: PASS (definitional — paging/scrolling + syntax highlighting recalled; line numbers under-emphasized). Advanced 7d → 14d. Wiki: [[MIT Missing Semester — Shell]]. |
| grep | procedure | developing | Basic File Tools | 2026-08-20 | 2026-08-27 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Reviewed 2026-08-20: pass (regex `*` repeats prev char, not glob match-anything). Advanced 3d → 7d. `grep pattern file` finds lines matching a regex pattern. `-r` for recursive search. Patterns are regular expressions. ripgrep is a faster alternative that searches recursively by default. `grep -c` counts matching lines directly. grep is line-by-line — can't match across newlines (multiline HTML breaks `<h3>.*</h3>`). In regex `*` repeats the previous char, NOT glob's match-anything. Wiki: [[MIT Missing Semester — Shell]]. |
| ripgrep | memory | developing | grep | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `ripgrep` (rg) is a faster, more human-friendly `grep` alternative. Recursively searches cwd by default. Supports regex. Less portable than grep. Recursive by default + respects `.gitignore` (skips ignored files) = fast & focused; plain grep isn't recursive and has no ignore awareness. Advanced 7d → 14d 2026-08-19 (pass). Wiki: [[MIT Missing Semester — Shell]]. |
| sed (Stream Editor) | procedure | developing | grep | 2026-08-25 | 2026-09-08 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | Reviewed 2026-08-23: HELD 7d (definitional) — `-i` correct (edit in place), but `/g` misread as "do nothing else": `/g` = GLOBAL, replace EVERY match on the line, not just the first. `sed -i 's/pattern/replacement/g' file` — find/replace with regex inline. `-i` modifies file, `/g` replaces all occurrences per line. Has its own mini programming language. Reviewed 2026-08-25 (mistake retry #1): PASS — predicted single-vs-global replacement correctly and placed /g after the final delimiter; 'all occurrences IN THE LINE' framing landed. Mistake → review (1 correct recall; graduates on next). Advanced 7d → 14d. Wiki: [[MIT Missing Semester — Shell]]. |
| find | procedure | developing | Shell Navigation & Paths | 2026-08-25 | 2026-08-28 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `find <dir> -type f -name "*.zip"` — recursive file search by type, name, size, age. `-exec` runs a command on each match; `{}` becomes the path, and the terminator decides batching — `\;` runs the command once PER file, `{} +` batches ALL matches into ONE invocation (`find . -name "*.txt" -exec wc -l {} +`). fd is a more human-friendly alternative. `-name "*.*"` filters to files with a dot (excludes extensionless like README). `-print0` emits NUL-delimited paths for `xargs -0`. `find` searches by ATTRIBUTES (type/name/size/age), not content. Quotes around `"*.zip"` prevent GLOB expansion (shell would expand `*` into cwd matches before find runs) — the opposite of word-splitting. Held 7d 2026-08-19 (quotes/glob misconception). Reviewed 2026-08-24 retry: FAIL — inverted again (claimed quotes ENABLE glob expansion; unquoted looks for literal "*.zip"); live demo showed unquoted glob expands to real filenames pre-find ("paths must precede expression"). Frame: quoting controls what bash does BEFORE the program sees it. Reviewed 2026-08-25 retry #3: FAIL again — walked the expansion slowly (*.zip rewritten by bash pre-find; unquoted filter collapses to one real filename → silent empty result); execution movie still inverts under pressure. Retry 2026-08-28. Wiki: [[MIT Missing Semester — Shell]]. |
| fd | memory | developing | find | 2026-08-26 | 2026-08-29 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `fd` is a faster, more intuitive `find` replacement. Simpler syntax, sensible defaults (e.g. ignores .gitignore files). Less portable than find. Wiki: [[MIT Missing Semester — Shell]]. |
| awk | procedure | developing | grep, sed | 2026-08-18 | 2026-09-01 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | Column-oriented data processing. `awk '{print $2}'` extracts 2nd whitespace-delimited field; `-F,`/`FS` sets input separator, `OFS` output separator (set in `BEGIN`). `$0`=whole line, `$1`/`$2`=fields, `NF`=field count. `~`/`!~` match operators: `$1 ~ /re/`. Pattern/action structure: `awk '/p/ {a}'`. FPAT handles fields containing delimiters (quoted CSV): `-v FPAT='[^,]+|"[^"]*"'`. `awk -F. '{print $NF}'` = extension after final dot (no-dot files → NF=1 → whole filename). Full pattern-matching language, not just a column picker. Wiki: [[MIT Missing Semester — Shell]]. |
| Pipes (`|`) & Pipeline Composition | concept | developing | What is the Shell | 2026-08-18 | 2026-09-01 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `|` connects stdout of one program to stdin of another. The shell's core composability mechanism. Pipes let you chain tools like grep, awk, sort, uniq without intermediate files. **Pipeline stages run in PARALLEL — all commands start at once and stream line-by-line** (as one writes, the next consumes instantly). Top-counts pattern: `sort | uniq -c | sort -nr | head -n 5` — uniq -c needs consecutive dupes (sort first); sort -nr = numeric desc. Wiki: [[MIT Missing Semester — Shell]]. |
| xargs | concept | developing | Pipes (`|`) & Pipeline Composition | 2026-08-27 | 2026-08-30 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | Converts stdin lines into command arguments: `find . -type f -name "*.sh" | xargs wc -l` counts lines inside each .sh file (without xargs, `wc -l` counts filenames). Default xargs splits on whitespace → filenames with spaces break into multiple args. Fix: `find -print0 | xargs -0` — NUL can never appear in a filename. Wiki: [[MIT Missing Semester — Shell]]. |
| curl (Web Fetching) | procedure | developing | None | 2026-08-27 | 2026-09-10 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `curl -s URL` fetches a webpage and prints raw HTML to stdout (-s silences the progress meter). Websites are streams — pipe into grep to scrape/count: `curl -s URL | grep -c 'href="/2026/'`. grep is line-by-line: multiline HTML breaks patterns; target a unique single-line attribute. **`curl | bash` hazard (enriched 2026-08-24):** piping straight into an interpreter executes unaudited code with your privileges — download first, inspect, then run: `curl -fsSL URL -o install.sh && less install.sh && bash install.sh`. The `bash -c "$(curl ...)"` variant forces bash semantics (survives "bashisms") but is still blind execution — inspection is the safety step. Wiki: [[MIT Missing Semester — Shell]]. |
| jq | procedure | developing | None | 2026-08-26 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | JSON processor for the shell: `jq .` pretty-prints to inspect structure, `.[] | select(.version > 6) | .name` unrolls array → filters → extracts field. Keep all jq pipes INSIDE the single quotes (outside, bash runs select/name as commands). `-r` outputs raw strings without quotes for further piping. Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Redirections & Streams | concept | developing | What is the Shell | 2026-08-25 | 2026-09-08 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | Streams: 0=stdin, 1=stdout, 2=stderr. `>` redirects stdout to file, `>>` appends, `<` feeds file as stdin, `2>` redirects stderr. `2>&1` merges stderr where stdout is going — order matters (left-to-right eval). `&>` is bash shortcut for both stdout+stderr. `/dev/null` = the null device — a special system file that DISCARDS everything written to it; `cmd > /dev/null 2>&1` fully silences a command, `2>/dev/null` drops only errors. Enriched 2026-08-24. `tee` writes to file AND stdout. **`cp` vs `>`:** `cp file1 file2` copies a file (photocopy — original untouched). `>` writes a command's stdout to a file (overwrites target). Using `notes.txt > notes.txt` doesn't copy — it reads nothing from stdin and overwrites the file to empty. Reviewed 2026-08-25: PASS (definitional) + first Feynman ✓ of the session — plumbing rerouted at process birth; cp photocopies vs > overwrites landed clean. Advanced 7d → 14d. Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Conditionals | procedure | developing | Shell Navigation & Paths | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `if condition; then ...; else ...; fi`. Condition is a command — `test -f file` or `[ -f file ]` check file existence. `[[ -f file && -r file ]]` is the modern bash version with &&/|| and regex matching. **Always quote variables in tests:** `[ -f "$FILE" ]` — without quotes, filenames with spaces split into multiple arguments and break syntax. `[` is a shell builtin (≡ test) whose exit status IS the condition; quotes prevent word-splitting. Advanced 7d → 14d 2026-08-19 (pass). Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Loops | procedure | developing | Shell Conditionals | 2026-08-17 | 2026-08-31 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `for var in list; do ...; done` iterates over values. `while condition; do ...; done` loops while condition succeeds. Use `{1..5}` or `$(seq 5)` to generate ranges. Always quote variables inside loops — `"$FILE"` protects against spaces. **Backup loop pattern:** `for FILE in *.txt; do cp "$FILE" "${FILE%.txt}_$(date +%Y-%m-%d).txt"; done` — combines glob, parameter expansion, and command substitution. Wiki: [[MIT Missing Semester — Shell]]. |
| Command Substitution & Arithmetic | procedure | developing | Shell Navigation & Paths | 2026-08-24 | 2026-09-07 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `$(command)` embeds command output inline — preferred over backticks (they nest poorly). `$((expr))` returns arithmetic result as string: `echo $((10 + 5))` outputs 15. `((expr))` evaluates for side effects (`((RUN++))`, `if ((x > 5))`). No `$` prefix needed for vars inside `(( ))`. **In filenames:** `cp notes.txt notes_$(date +%Y-%m-%d).txt` — the subshell runs `date`, outputs today's date, and it gets embedded into the target filename. Reviewed 2026-08-24: PASS (discriminative) — $(cmd) forks a subshell, runs the program, splices its stdout into the string; $(( )) is bash-internal arithmetic, NO process launched. Labels first round, machine model second round. Advanced 7d → 14d. Wiki: [[MIT Missing Semester — Shell]]. |
| Shebang & Script Execution | procedure | developing | What is the Shell | 2026-08-25 | 2026-08-28 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `#!/bin/bash` on line 1 tells the kernel which interpreter to use. Save as script.sh, run with `./script.sh` after `chmod +x script.sh`. Or run directly with `bash script.sh`. **Safety flags deep dive:** `set -e` (errexit) — script stops immediately on any command failure, prevents cascading disasters (e.g. `cd` fails then `rm -rf *` runs in wrong dir). `set -u` (nounset) — exit on unset variable; prevents typos like `$VAR_DIER` evaluating to empty and turning `rm -rf` into `rm -rf /*`. `set -o pipefail` — pipeline returns failure if ANY command in chain fails, not just the last (e.g. `grep` returns 1 when no match, kills pipeline). `set -x` (xtrace) — prints expanded commands before running; **security risk**: exposes secrets like `Authorization: $TOKEN` in logs/terminal. `set +x` temporarily disables tracing around sensitive code. **`|| true` pattern:** append to suppress expected errors so `set -e` doesn't kill script (e.g. `grep "alice" file || true`). Reviewed 2026-08-25: FAIL — thought `#!` means 'look in this directory'; missed the two-route split: ./script = kernel executes file directly (needs x bit; shebang = absolute interpreter path), bash script = bash READS the file (needs only r; shebang is just a comment). First review since 08-04. Retry 2026-08-28. Wiki: [[MIT Missing Semester — Shell]]. |
| Background Jobs | concept | developing | What is the Shell | 2026-08-20 | 2026-08-27 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Reviewed 2026-08-20: pass (bg job still child of shell → SIGHUP on close; nohup before / disown after). Advanced to 7d. `&` runs command in background, freeing the terminal. `$!` holds PID of most recent background job. `kill $PID` terminates it. `jobs` lists background jobs; `fg %1` brings to foreground. Essential for parallel execution and process mgmt in scripts. Wiki: [[MIT Missing Semester — Shell]]. |
| Signals (Software Interrupts) | concept | developing | Background Jobs | 2026-08-21 | 2026-08-28 | https://missing.csail.mit.edu/2026/command-line-environment/ | discriminative | A signal is an asynchronous "software interrupt" the kernel delivers to a process — it may catch, ignore, or take the default (often terminate). Kill ladder: SIGINT (Ctrl-C, catchable) → SIGTERM (`kill`, catchable → graceful cleanup) → SIGKILL (`kill -9`, UNcatchable, immediate, may orphan children / skip cleanup). That's why Ctrl-C sometimes fails and why kill -9 is a last resort. **You only use a few signals in practice** — SIGINT (interrupt, the one reached for most), SIGQUIT (Ctrl-\), SIGTERM (graceful kill), SIGKILL (force), SIGTSTP (suspend), SIGHUP (hangup) — know the common set, not the whole list. Reviewed 2026-08-21: PASS (discriminative — full ladder + why kill -9 skips cleanup; refined: SIGINT/SIGTERM catchable, SIGKILL UNcatchable). Advanced 3d → 7d. Wiki: [[MIT Missing Semester — Command-line Environment]]. |
| Job Control (Ctrl-Z, fg/bg, nohup/disown) | procedure | developing | Background Jobs | 2026-08-21 | 2026-08-28 | https://missing.csail.mit.edu/2026/command-line-environment/ | definitional | Ctrl-Z sends SIGTSTP (suspend, not kill); `fg %1`/`bg %1` resume; `jobs` lists. `&` backgrounds from the start, `$!` = last bg PID. Backgrounded jobs are still children of the terminal → closing it sends SIGHUP and kills them; survive with `nohup cmd &` (before) or `disown %1` (after). Reviewed 2026-08-21: PASS (definitional — nailed why disown uses %job-number vs nohup takes the command: nohup is a wrapper before the job exists, disown references an already-existing job-table slot). Advanced 3d → 7d. Wiki: [[MIT Missing Semester — Command-line Environment]]. |
| SSH: Public-Key Auth & Remote Commands | concept | developing | Pipes, Shell Built-ins & Process Isolation | 2026-08-25 | 2026-09-08 | https://missing.csail.mit.edu/2026/command-line-environment/ | discriminative | SSH uses public-key crypto: the **public key** is shareable (server uses it to verify you), the **private key** is your secret — NEVER share it (≈ password). `ssh host cmd` runs a command on the remote machine; its stdout streams back. **Quoting decides where the pipe runs:** `ssh host ls | wc -l` runs `ls` remotely + counts locally; `ssh host 'ls | wc -l'` runs the whole pipeline remotely (only the result crosses the wire). **Passphrase (enriched 2026-08-19):** a passphrase encrypts the private key FILE itself; without one the key is plain text, so anyone with filesystem access can copy it and use it to log into your servers (no password needed). Check/validate with `ssh-keygen -y -f /path/to/key`. **Key-based auth handshake (corrected 2026-08-23):** sign/verify challenge–response — (1) client presents public key (in `~/.ssh/authorized_keys`); (2) server sends a *plaintext* random challenge; (3) client **signs** the challenge with its private key, returns the signature; (4) server **verifies** the signature with the public key — access if valid. (Lecture notes' "server encrypts with public key" framing is inaccurate — corrected to match MIT Security lecture.) **Private key never travels the wire.** **Client config (enriched 2026-08-24):** `~/.ssh/config` declares host aliases + defaults (HostName/User/IdentityFile/LocalForward) and is ALSO read by scp/rsync/mosh — beats shell aliases, which only the shell sees. (Notes' encrypt-the-challenge framing recurred — still wrong; sign/verify stands.) Reviewed 2026-08-25: PASS (discriminative) — quote-decides-pipe-location instant (4th straight pass, mastery 1.00); added: push computation to the data — ship the question, not the dataset (unquoted pipe ships the whole listing; a dropped connection makes local wc silently undercount). Advanced → 14d. Wiki: [[SSH — Public-Key Auth & Remote Commands]]. |
| Shell Positional & Special Parameters | memory | developing | What is the Shell | 2026-08-26 | 2026-09-02 | https://missing.csail.mit.edu/2020/shell-tools/ | discriminative | Arguments arrive as plain strings; inside a script reach them via special syntax: `$0` = program name, `$1`–`$9` = positional args (10th+ needs braces `${10}`), `$#` = argument count, `$@` = all args as a list. Unquoted `$@` word-splits on spaces — quote it (`"$@"`) to preserve each argument. Wiki: [[Positional & Special Shell Parameters]]. |
| Process Substitution | concept | developing | Shell Redirections & Standard Streams | 2026-08-25 | 2026-08-28 | https://missing.csail.mit.edu/2020/shell-tools/ | definitional | `<(CMD)` runs CMD, sends its output to a temp file/pipe, and substitutes that **file path** (e.g. `/dev/fd/63`) where the text sits — so commands that expect a *file path* (not stdin) can consume command output: `diff <(ls a) <(ls b)`. NOT variable capture (that is command substitution `$(CMD)`). Lecture notes erroneously said `<(CMD)` captures into a variable — flagged. Reviewed 2026-08-25: FAIL (discriminative) — read $(ls A) $(ls B) as a 'combined list' (missed word-split melting the A/B boundary); wrapped BOTH listings in one <() so bash tried to execute filenames → empty pipes → diff falsely reported identical. Anchor: $() substitutes TEXT (then splits), <() substitutes a PATH (/dev/fd/N). Retry 2026-08-28. Wiki: [[Process Substitution]]. |
| Shell Config & Dotfiles | concept | developing | What is the Shell, Shell Built-ins & Process Isolation | 2026-08-23 | 2026-08-30 | https://missing.csail.mit.edu/2026/command-line-environment/ | discriminative | Reviewed 2026-08-23: PASS (discriminative) — core mechanism right: hand-typed export lives only in the running shell process's memory and dies with it at logout; a login shell reads startup files, `source` runs the file in the CURRENT shell (no child). Advanced 3d → 7d. Hand-typed shell settings don't survive logout — persist them in the shell config file (`~/.bashrc` / `~/.bash_profile`), which the shell reads on startup. `source ~/.bashrc` evaluates the file line by line in the CURRENT shell (no re-login needed). `PS1` renders the left prompt — the classic first dotfile tweak. **Dotfiles** (hidden plain-text config files, names start with `.`): collect them in one version-controlled folder (~/.dotfiles) and `ln -s` them into the standard locations via an install script → portable environment on any new machine. **Plugin frameworks** (Oh My Zsh) can bloat/slow the shell — add one plugin at a time. `command-not-found.com` shows which package provides a command. **Startup modes (enriched 2026-08-24):** which config loads = login-vs-non-login × interactive-vs-non-interactive. Login shells run the profile flow (`/etc/profile` → `~/.bash_profile`, fallbacks `~/.bash_login`/`~/.profile`); interactive non-login read `~/.bashrc`; non-interactive shells (scripts/cron) SKIP user startup files for speed & clean runs. Lecture inverted the non-login mapping — corrected here. Wiki: [[MIT Missing Semester — Shell Configuration & Dotfiles]]. |
| File Permissions (ls -l) | memory | developing | ls & File Listing | 2026-08-24 | 2026-09-07 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | The 10-char string from `ls -l`: char 1 = file type (d=directory, -=regular file, l=symlink). Chars 2-4 = Owner rwx, 5-7 = Group rwx, 8-10 = Others rwx. `-` means permission denied. e.g. `drwxr-xr-x` = directory, owner full access, group/others read+execute only. **`chmod +x file`** adds execute permission — required to run scripts via `./script.sh`. Without it, Bash throws "Permission denied". Reviewed 2026-08-24: PASS (definitional) — rwx decode + group/others bits right; framing corrected: char 1 = file TYPE, then chars 2-4 owner / 5-7 group / 8-10 others (not "first four = owner"); formal term "others", not guests. First review since 08-04. Wiki: [[MIT Missing Semester — Shell]]. |
| Wildcards & Globs | memory | developing | Basic File Tools | 2026-08-11 | 2026-08-25 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `*` matches zero or more chars (e.g. `report*` matches report.pdf). `?` matches exactly one char (e.g. `c?t.png` matches cat.png but not cart.png). `[abc]`/`[0-9]` matches one char from a set. `{a,b}` is brace expansion — generates combinations (e.g. `file_{draft,final}.txt`), does NOT match existing files. Wiki: [[MIT Missing Semester — Shell]]. |
| Bash Quoting | concept | developing | Shell Navigation & Paths | 2026-08-25 | 2026-09-24 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Reviewed 2026-08-20: held 7d — flipped quoting semantics (thought ANSI-C `$'...'` expands vars; actually DOUBLE quotes `"..."` expand $/$(cmd); single `'...'` = total literal, ANSI-C = escapes only). Three types: Single `'...'` = absolute literal, no expansion of $, !, escapes. Double `"..."` = allows $USER, $(cmd), \ escapes, but protects spaces/wildcards. ANSI-C `$'...'` = processes \n, \t escape sequences, $ and ! treated as literal. Reviewed 2026-08-25: PASS (discriminative) — placed all three expansions correctly; 'escapes-only' framing for ANSI-C stuck. Advanced 14d → 30d; mistake moves to review (1 correct recall). Wiki: [[MIT Missing Semester — Shell]]. |
| Parameter Expansion | procedure | developing | Shell Loops | 2026-08-27 | 2026-08-30 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `${var%pattern}` removes matching suffix, `${var#pattern}` removes matching prefix. E.g. `${FILE%.txt}` strips `.txt` extension. `${file##*/}` strips everything before last `/` (basename). `${var:-default}` provides fallback if var is unset. Combined with loops and command substitution for file manipulation. Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Built-ins & Process Isolation | concept | developing | Shell Navigation & Paths | 2026-08-26 | 2026-09-09 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | **Why cd is a built-in:** child processes inherit parent's memory but CANNOT modify it — OS enforces memory isolation. If `cd` ran as external program, it would change its own working directory, then exit — shell's `$PWD` unchanged. **Internal shell state:** `$PWD`, env vars (`PATH`, `USER`), background jobs, shell options (`set`, `shopt`) — all live in shell process RAM. **Built-ins vs external:** `cd`, `export`, variable assignment, `source`, `set` must be built-ins because they mutate shell state. `ls`, `mkdir`, `grep` work as external because they create filesystem side-effects (disk writes, stdout), not shell memory changes. Wiki: [[MIT Missing Semester — Shell]]. |
| Makefile: Targets, Prerequisites & Recipes | concept | developing | None | 2026-08-12 | 2026-08-19 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | A Makefile rule: `target : prerequisites` then indented `recipe`. Target = output file (e.g. monitor) or action (e.g. `clean`). Prerequisites = input files (e.g. main.c). Recipe = shell commands — **every recipe line must start with a tab** (spaces → error). Rule can have no prerequisites but must have a recipe. Non-goal rules only run when invoked explicitly (e.g. `make clean`). Wiki: [[MIT Missing Semester — Shell]]. |
| Make: Timestamp Evaluation | concept | developing | Makefile: Targets, Prerequisites & Recipes | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | make compares modification timestamps: if target doesn't exist → rebuild. If any prerequisite is newer than target → rebuild. If all prerequisites older → skip ("up to date"). This is make's core efficiency mechanism. Add Makefile itself to a target's prerequisites to rebuild when the Makefile changes — make never inspects content, only mtimes: even a comment-only save bumps the timestamp and forces recompilation. Wiki: [[MIT Missing Semester — Shell]]. |
| Make: Dependency Tree Resolution | concept | developing | Makefile: Targets, Prerequisites & Recipes | 2026-08-18 | 2026-09-01 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | make reads rules top-down to build a dependency tree in memory, but executes bottom-up (leaves first). If a prerequisite is missing, make recursively finds its rule and builds it first. The first rule is the default goal (what `make` builds when invoked with no arguments). Wiki: [[MIT Missing Semester — Shell]]. |
| Make Variables (CC, CFLAGS) | memory | developing | Makefile: Targets, Prerequisites & Recipes | 2026-08-08 | 2026-08-12 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Variables in Makefiles: `CC = gcc`, `CFLAGS = -Wall -Wextra -std=c17 -g`. Referenced in recipes as `$(CC)` and `$(CFLAGS)`. Change once at top instead of editing every recipe line. `CPPFLAGS = -Isrc -Ithird_party` carries include search paths — that's how `#include "meminfo.h"` / `#include "acutest.h"` resolve. Test-target pattern (Lesson 3): executable depends on test file + module under test + headers; `test:` target runs `./tests/test_meminfo` and rebuilds only when those change. Wiki: [[MIT Missing Semester — Shell]]. |
| Clean Targets & .PHONY | concept | developing | Makefile: Targets, Prerequisites & Recipes | 2026-08-08 | 2026-08-15 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `clean:` target has no prerequisites, recipe is `rm -f monitor`. `.PHONY: clean` tells make that `clean` is NOT a real file — always run its recipe even if a file named clean exists on disk. Without .PHONY, make would skip the recipe if a file named clean exists (thinks it's up to date). Phony targets avoid name conflicts and improve performance. Default goal = first target in Makefile (targets starting with `.` are skipped unless they contain `/`). Wiki: [[MIT Missing Semester — Shell]].
| Git commit message conventions | memory | developing | What is the Shell | 2026-08-25 | 2026-09-08 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Conventional commits: `<type>(<scope>): <subject>` — type tells what kind of change (feat/fix/docs/refactor/chore), scope narrows context, subject is imperative mood ≤50 chars. Body explains *why*, not what. Footer references issues and breaking changes. Scannable git log. Reviewed 2026-08-25: PASS (discriminative) — picked conventional entry instantly, justified by specificity (what changed + where); added machine-scannability framing (`git log --grep="fix(auth):"`) and imperative mood (fix not fixed). First review since 08-04. Advanced 30d → 14d slot. Wiki: [[MIT Missing Semester — Shell]]. |
| Environment Variables (Shell) | concept | developing | Shell Built-ins & Process Isolation | 2026-08-27 | 2026-09-03 | https://missing.csail.mit.edu/2026/command-line-environment/ | discriminative | Per-process `KEY=value` string bags passed parent → child. Three scopes: `TZ=Asia/Tokyo date` (that child only — `echo $TZ` empty afterwards), plain `FOO=1` (shell-local; children do NOT inherit), `export DEBUG=1` (in env → every child inherits; `bash -c 'echo $DEBUG'` → 1). `unset VAR` deletes. export/unset/assignment must be built-ins — children can't mutate parent memory (process isolation). `env` lists them; `$PATH` is just an env var. Wiki: [[Environment Variables (Shell)]]. |
| Exit Codes & Short-Circuit Control Flow | concept | developing | Shell Conditionals | 2026-08-24 | 2026-08-28 | https://missing.csail.mit.edu/2026/command-line-environment/ | definitional | Convention 0 = success, non-zero = failure. `exit NUM` built-in chooses it; `$?` reads the LAST command's code (asking twice gives 0 the second time — echo succeeded). Guard: `mkdir /root/secret || exit 1`. Unlike boolean operators in regular languages, `&&`/`||` act on return CODES and short-circuit: right side runs only when demanded. `if`/`while` conditions ARE commands judged by exit status. Pair with `set -e` for fail-fast. Wiki: [[Exit Codes & Short-Circuit Control Flow]]. |
| tmux — Sessions, Windows & Panes | memory | developing | Background Jobs | 2026-08-24 | 2026-08-29 | https://missing.csail.mit.edu/2026/command-line-environment/ | definitional | Hierarchy: **Session** (independent workspace, survives detach/disconnect — durable cousin of background jobs) > **Window** (full-terminal view ≈ editor/browser tabs, usually separate parts of one session) > **Pane** (split screens INSIDE one window, several visible at once). Keeps long-running work alive across SSH drops. Starter page — deepen in Command-line Environment lesson. Wiki: [[tmux — Sessions, Windows & Panes]]. |
| File Transfers over SSH (scp/rsync/ssh config) | procedure | developing | SSH: Public-Key Auth & Remote Commands | 2026-08-24 | 2026-08-30 | https://missing.csail.mit.edu/2026/command-line-environment/ | definitional | scp = straight remote copy. rsync IMPROVES ON scp (not "builds on"): skips identical files (delta-aware), finer symlink/permission control, resumable via `-P` (= `--partial --progress`; MIT cites --partial). `~/.ssh/config` declares host aliases + defaults (HostName/User/IdentityFile/Port) and is read not only by ssh but also scp/rsync/mosh — beats shell aliases, which only the shell sees. Wiki: [[File Transfers over SSH — scp, rsync & ssh_config]]. |
| less (Pager) | memory | developing | None | 2026-08-27 | 2026-09-03 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Terminal pager: views files or command output one screen/page at a time (`q` quits, `/pat` searches). Unlike the older `more` utility and unlike editors, `less` does NOT load the whole file into memory first — lazy reading makes it extremely fast and memory-efficient on massive log/config files. Pipe any command's output into it to paginate. Wiki: [[less (Pager)]]. |
| Vim Modal Editing | concept | developing | Shell Built-ins & Process Isolation | 2026-08-28 | 2026-08-31 | https://missing.csail.mit.edu/2020/editors/ | definitional | Vim is a **modal editor**: it separates inserting text from navigating/editing, because programmers spend most time reading/editing rather than writing long streams. The same keystroke means different things per mode (`x` inserts a literal 'x' in Insert mode, deletes the char under the cursor in Normal mode, deletes the selection in Visual). Modes: Normal (navigate/edit — the default), Insert (`i`), Replace (`R`), Visual (`v`/`V`/`Ctrl-v`), Command-line (`:`). `Esc` returns to Normal from anywhere. Live in Normal mode; dip into Insert only to type new text. Wiki: [[MIT Missing Semester — Editors (Vim)]]. |
| Vim Composable Commands | concept | developing | Shell Built-ins & Process Isolation | 2026-08-28 | 2026-08-31 | https://missing.csail.mit.edu/2020/editors/ | definitional | Vim's UI is a programming language: editing commands are **verbs** (`d` delete, `c` change, `y` yank/copy), movements are **nouns** (`w` next word, `b` back, `e` end, `$` EOL, `0` BOL), composed as verb+noun+count+modifier. `dw` delete word; `cw` change word (delete then Insert); `3w` move 3 words; `7dw` delete 7 words. Modifiers `i`=**in**side vs `a`=**a**round: `ci(` change INSIDE the parens (delimiters stay), `da'` delete a quoted string AND its quotes. Motions `f`=find (onto char), `t`=un**t**il (up to, not including): `ct)` deletes from cursor to just before the next `)`. `u` undo, `Ctrl-r` redo, `p` paste. Wiki: [[MIT Missing Semester — Editors (Vim)]]. |
| Vim Buffers & Windows | concept | developing | Shell Built-ins & Process Isolation | 2026-08-28 | 2026-08-31 | https://missing.csail.mit.edu/2020/editors/ | definitional | **Buffer** = an open file in memory; **window** = a view onto a buffer; **tab** = a collection of windows. No 1:1 buffer↔window mapping — one buffer can show in multiple windows at once (`:sp`/`:vsp` to view two parts of the same file). Closing a window (`:q`) does NOT close the buffer. `:e file` open, `:ls` list buffers, `:w` write, `:wq` save+quit. Wiki: [[MIT Missing Semester — Editors (Vim)]]. |

### Curriculum covered-markers (Stage 0)

> Lessons already covered by live concepts below — marked `done` in `CURRICULUM.md`. When the curriculum reaches a covered lesson, run a **retrieval check** to verify (never re-teach); on failure, demote the lesson back to `in-progress` and correct the concept status.
>
> **A1 First compile (make)** ↔ Makefile: Targets/Prerequisites/Recipes, Make Variables (CC, CFLAGS), Clean Targets & .PHONY

---

## Mastery Summary

- **SWE (Software Engineering Fundamentals):** 43 developing (Shell & Terminal + Makefiles from MIT Missing Semester + Git conventions + signals & job control + SSH + shell config & dotfiles + Vim modal editing/composable commands/buffers)
- **Not Started:** 0
- **Paused:** ~103 (archived on 2026-07-22 during roadmap shift; C-project + testing-methodology concepts archived 2026-08-25 — see `📦 Concept Archive.md`)
- **Consolidated:** 0
- **Total concepts tracked:** ~118

---

## Open Questions

> Questions that emerged during sessions but haven't been fully resolved yet. The assistant surfaces these at the start of every session.

- None currently