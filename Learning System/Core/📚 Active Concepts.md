# KNOWLEDGE BASE — Active Concepts

> Purpose: Active learning record for the current focus area. The assistant reads this at the start of learning sessions.
> Paused/archived concepts: `Learning System/Core/📦 Concept Archive.md` (searched on demand, not auto-loaded)
> Scripture memory: `Learning System/Core/📖 Scripture Memory.md`

## Metadata

- **Tracks:** SWE (Software Engineering Fundamentals) — Shell & Terminal
- **Learner:** Aheebwa Delight
- **Source:** MIT Missing Semester — https://missing.csail.mit.edu/2026/course-shell/
- **Previous Track:** AI Engineering (aie) — archived 2026-07-28
- **Last Updated:** 2026-08-21
- **Interleaving:** Active (shuffle + adjacency constraint + alternating question types)
- **System:** Open WebUI Learning System

---

## Live System Notes

- Use **"swe"** to trigger Software Engineering track reviews
- Use **"lesson"/"continue"** to run the next Stage-0 curriculum lesson (see `Learning System/CURRICULUM.md`; delegated to the `learning-teach` skill — probe → plan → teach, live fact-checking, no Mimo gate on teaching)
- Each trigger runs a separate review session limited to that track's due concepts (cap of 5 per session)
- `Sessions/` is the session history for the active learning system
- `Reviews/` stores spaced-repetition review notes
- `Concept Notes/` stores reusable atomic concept pages
- `Archive/` and `📦 Concept Archive.md` are reference-only
- This file is the source of truth for what is due and what is developing
- No more than 5 review concepts per session; overflow stays queued
- Previous tracks (aie, makemore) archived on 2026-07-28 — all reviews paused
- **Consolidation:** When a concept reaches `consolidated` status, it is moved to `Archive/Consolidated/[name].md` with a link to its Knowledge Wiki note and removed from this table.

---

## SWE Track — Shell & Terminal (MIT Missing Semester)

> Concepts from MIT Missing Semester Lecture 1: Course Overview + Introduction to the Shell
> Status values: `not_started` → `developing` → `pending_mastery` → `mastered` → `consolidated`
> Staggered initial reviews: staggered entry, then 3d → 7d → 14d → 30d → 90d → consolidated

| Concept | Status | Prerequisites | Last Reviewed | Next Review | Source | Last Q Type | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| What is the Shell | developing | None | 2026-08-21 | 2026-08-28 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | The shell is a textual interface to run programs, combine them via pipes/redirects, and automate workflows. Terminal app → shell prompt (missing:~$), where $=normal user, #=root. Run any program by name. Reviewed 2026-08-21: MOSTLY PASS (discriminative) — decoded machine/~/normal-user correctly; MISFIRE: called `#` a "guest" — it is root/superuser (the opposite; max privileges). Prompt tells you how dangerous your next command could be. Held 7d. Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Navigation & Paths | developing | What is the Shell | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | cd (built-in, not a program), pwd, paths: absolute (start /) vs relative (from cwd). `.` = this dir, `..` = parent, `~` = home. Tab completion works. zoxide for faster cd. Reviewed 2026-08-19: built-in vs external distinction — `cd` must be a built-in (a child process can't propagate a cwd change back); `pwd` is also a built-in in bash; `ls` is external. Wiki: [[MIT Missing Semester — Shell]]. |
| man & Documentation | developing | What is the Shell | 2026-08-19 | 2026-08-26 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `man <program>` opens the manual page. `--help` is a quick flag for most commands. tldr shows practical examples. LLMs also work well for explaining commands. The 4-layer ladder: man (authoritative) → --help (quick flag list) → tldr (worked examples) → LLM (conversational). Reviewed 2026-08-19: held at 7d — had tldr/--help but missed man as the primary layer. Wiki: [[MIT Missing Semester — Shell]]. |
| PATH & Program Discovery | developing | What is the Shell | 2026-08-04 | 2026-08-11 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | Shell consults `$PATH` env var — a list of directories searched in order when a command is typed. `which <program>` shows which file is executed. Bypass PATH by giving the full path. `ls /bin` shows available programs. Wiki: [[MIT Missing Semester — Shell]]. |
| ls & File Listing | developing | Shell Navigation & Paths | 2026-08-15 | 2026-08-29 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `ls <path>` lists directory contents. `ls /bin` shows every program on PATH. `ls -l` for detailed listing (permissions, size, date). eza is a more human-friendly alternative. Wiki: [[MIT Missing Semester — Shell]]. |
| Basic File Tools (cat, sort, uniq, head, tail) | developing | What is the Shell | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `cat file` prints file contents. `sort file` sorts lines. `uniq file` removes consecutive duplicates. `head`/`tail` show first/last lines. bat is a cat alternative with syntax highlighting. `uniq` only collapses consecutive duplicates — true dedup/count needs `sort | uniq -c`. Advanced 7d → 14d 2026-08-19 (pass). Wiki: [[MIT Missing Semester — Shell]]. |
| bat | developing | Basic File Tools | 2026-08-21 | 2026-09-04 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `bat` is a drop-in replacement for `cat` with syntax highlighting, line numbers, and paging/scrolling. Install from https://github.com/sharkdp/bat. Reviewed 2026-08-21: PASS (definitional — paging/scrolling + syntax highlighting recalled; line numbers under-emphasized). Advanced 7d → 14d. Wiki: [[MIT Missing Semester — Shell]]. |
| grep | developing | Basic File Tools | 2026-08-20 | 2026-08-27 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Reviewed 2026-08-20: pass (regex `*` repeats prev char, not glob match-anything). Advanced 3d → 7d. `grep pattern file` finds lines matching a regex pattern. `-r` for recursive search. Patterns are regular expressions. ripgrep is a faster alternative that searches recursively by default. `grep -c` counts matching lines directly. grep is line-by-line — can't match across newlines (multiline HTML breaks `<h3>.*</h3>`). In regex `*` repeats the previous char, NOT glob's match-anything. Wiki: [[MIT Missing Semester — Shell]]. |
| ripgrep | developing | grep | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `ripgrep` (rg) is a faster, more human-friendly `grep` alternative. Recursively searches cwd by default. Supports regex. Less portable than grep. Recursive by default + respects `.gitignore` (skips ignored files) = fast & focused; plain grep isn't recursive and has no ignore awareness. Advanced 7d → 14d 2026-08-19 (pass). Wiki: [[MIT Missing Semester — Shell]]. |
| sed (Stream Editor) | developing | grep | 2026-08-15 | 2026-08-22 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `sed -i 's/pattern/replacement/g' file` — find/replace with regex inline. `-i` modifies file, `/g` replaces all occurrences per line. Has its own mini programming language. Wiki: [[MIT Missing Semester — Shell]]. |
| find | developing | Shell Navigation & Paths | 2026-08-19 | 2026-08-26 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `find <dir> -type f -name "*.zip"` — recursive file search by type, name, size, age. `-exec` runs a command on each match. fd is a more human-friendly alternative. `-name "*.*"` filters to files with a dot (excludes extensionless like README). `-print0` emits NUL-delimited paths for `xargs -0`. `find` searches by ATTRIBUTES (type/name/size/age), not content. Quotes around `"*.zip"` prevent GLOB expansion (shell would expand `*` into cwd matches before find runs) — the opposite of word-splitting. Held 7d 2026-08-19 (quotes/glob misconception). Wiki: [[MIT Missing Semester — Shell]]. |
| fd | developing | find | 2026-08-05 | 2026-08-12 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `fd` is a faster, more intuitive `find` replacement. Simpler syntax, sensible defaults (e.g. ignores .gitignore files). Less portable than find. Wiki: [[MIT Missing Semester — Shell]]. |
| awk | developing | grep, sed | 2026-08-18 | 2026-09-01 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | Column-oriented data processing. `awk '{print $2}'` extracts 2nd whitespace-delimited field; `-F,`/`FS` sets input separator, `OFS` output separator (set in `BEGIN`). `$0`=whole line, `$1`/`$2`=fields, `NF`=field count. `~`/`!~` match operators: `$1 ~ /re/`. Pattern/action structure: `awk '/p/ {a}'`. FPAT handles fields containing delimiters (quoted CSV): `-v FPAT='[^,]+|"[^"]*"'`. `awk -F. '{print $NF}'` = extension after final dot (no-dot files → NF=1 → whole filename). Full pattern-matching language, not just a column picker. Wiki: [[MIT Missing Semester — Shell]]. |
| Pipes (`|`) & Pipeline Composition | developing | What is the Shell | 2026-08-18 | 2026-09-01 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `|` connects stdout of one program to stdin of another. The shell's core composability mechanism. Pipes let you chain tools like grep, awk, sort, uniq without intermediate files. **Pipeline stages run in PARALLEL — all commands start at once and stream line-by-line** (as one writes, the next consumes instantly). Top-counts pattern: `sort | uniq -c | sort -nr | head -n 5` — uniq -c needs consecutive dupes (sort first); sort -nr = numeric desc. Wiki: [[MIT Missing Semester — Shell]]. |
| xargs | developing | Pipes (`|`) & Pipeline Composition | 2026-08-18 | 2026-08-25 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | Converts stdin lines into command arguments: `find . -type f -name "*.sh" | xargs wc -l` counts lines inside each .sh file (without xargs, `wc -l` counts filenames). Default xargs splits on whitespace → filenames with spaces break into multiple args. Fix: `find -print0 | xargs -0` — NUL can never appear in a filename. Wiki: [[MIT Missing Semester — Shell]]. |
| curl (Web Fetching) | developing | None | 2026-08-08 | 2026-08-15 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `curl -s URL` fetches a webpage and prints raw HTML to stdout (-s silences the progress meter). Websites are streams — pipe into grep to scrape/count: `curl -s URL | grep -c 'href="/2026/'`. grep is line-by-line: multiline HTML breaks patterns; target a unique single-line attribute. Wiki: [[MIT Missing Semester — Shell]]. |
| jq | developing | None | 2026-08-17 | 2026-08-20 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | JSON processor for the shell: `jq .` pretty-prints to inspect structure, `.[] | select(.version > 6) | .name` unrolls array → filters → extracts field. Keep all jq pipes INSIDE the single quotes (outside, bash runs select/name as commands). `-r` outputs raw strings without quotes for further piping. Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Redirections & Streams | developing | What is the Shell | 2026-08-18 | 2026-08-25 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Streams: 0=stdin, 1=stdout, 2=stderr. `>` redirects stdout to file, `>>` appends, `<` feeds file as stdin, `2>` redirects stderr. `2>&1` merges stderr where stdout is going — order matters (left-to-right eval). `&>` is bash shortcut for both stdout+stderr. `tee` writes to file AND stdout. **`cp` vs `>`:** `cp file1 file2` copies a file (photocopy — original untouched). `>` writes a command's stdout to a file (overwrites target). Using `notes.txt > notes.txt` doesn't copy — it reads nothing from stdin and overwrites the file to empty. Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Conditionals | developing | Shell Navigation & Paths | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `if condition; then ...; else ...; fi`. Condition is a command — `test -f file` or `[ -f file ]` check file existence. `[[ -f file && -r file ]]` is the modern bash version with &&/|| and regex matching. **Always quote variables in tests:** `[ -f "$FILE" ]` — without quotes, filenames with spaces split into multiple arguments and break syntax. `[` is a shell builtin (≡ test) whose exit status IS the condition; quotes prevent word-splitting. Advanced 7d → 14d 2026-08-19 (pass). Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Loops | developing | Shell Conditionals | 2026-08-17 | 2026-08-31 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `for var in list; do ...; done` iterates over values. `while condition; do ...; done` loops while condition succeeds. Use `{1..5}` or `$(seq 5)` to generate ranges. Always quote variables inside loops — `"$FILE"` protects against spaces. **Backup loop pattern:** `for FILE in *.txt; do cp "$FILE" "${FILE%.txt}_$(date +%Y-%m-%d).txt"; done` — combines glob, parameter expansion, and command substitution. Wiki: [[MIT Missing Semester — Shell]]. |
| Command Substitution & Arithmetic | developing | Shell Navigation & Paths | 2026-08-04 | 2026-08-11 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `$(command)` embeds command output inline — preferred over backticks (they nest poorly). `$((expr))` returns arithmetic result as string: `echo $((10 + 5))` outputs 15. `((expr))` evaluates for side effects (`((RUN++))`, `if ((x > 5))`). No `$` prefix needed for vars inside `(( ))`. **In filenames:** `cp notes.txt notes_$(date +%Y-%m-%d).txt` — the subshell runs `date`, outputs today's date, and it gets embedded into the target filename. Wiki: [[MIT Missing Semester — Shell]]. |
| Shebang & Script Execution | developing | What is the Shell | 2026-08-04 | 2026-08-11 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `#!/bin/bash` on line 1 tells the kernel which interpreter to use. Save as script.sh, run with `./script.sh` after `chmod +x script.sh`. Or run directly with `bash script.sh`. **Safety flags deep dive:** `set -e` (errexit) — script stops immediately on any command failure, prevents cascading disasters (e.g. `cd` fails then `rm -rf *` runs in wrong dir). `set -u` (nounset) — exit on unset variable; prevents typos like `$VAR_DIER` evaluating to empty and turning `rm -rf` into `rm -rf /*`. `set -o pipefail` — pipeline returns failure if ANY command in chain fails, not just the last (e.g. `grep` returns 1 when no match, kills pipeline). `set -x` (xtrace) — prints expanded commands before running; **security risk**: exposes secrets like `Authorization: $TOKEN` in logs/terminal. `set +x` temporarily disables tracing around sensitive code. **`|| true` pattern:** append to suppress expected errors so `set -e` doesn't kill script (e.g. `grep "alice" file || true`). Wiki: [[MIT Missing Semester — Shell]]. |
| Background Jobs | developing | What is the Shell | 2026-08-20 | 2026-08-27 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Reviewed 2026-08-20: pass (bg job still child of shell → SIGHUP on close; nohup before / disown after). Advanced to 7d. `&` runs command in background, freeing the terminal. `$!` holds PID of most recent background job. `kill $PID` terminates it. `jobs` lists background jobs; `fg %1` brings to foreground. Essential for parallel execution and process mgmt in scripts. Wiki: [[MIT Missing Semester — Shell]]. |
| Signals (Software Interrupts) | developing | Background Jobs | 2026-08-21 | 2026-08-28 | https://missing.csail.mit.edu/2026/command-line-environment/ | discriminative | A signal is an asynchronous "software interrupt" the kernel delivers to a process — it may catch, ignore, or take the default (often terminate). Kill ladder: SIGINT (Ctrl-C, catchable) → SIGTERM (`kill`, catchable → graceful cleanup) → SIGKILL (`kill -9`, UNcatchable, immediate, may orphan children / skip cleanup). That's why Ctrl-C sometimes fails and why kill -9 is a last resort. **You only use a few signals in practice** — SIGINT (interrupt, the one reached for most), SIGQUIT (Ctrl-\), SIGTERM (graceful kill), SIGKILL (force), SIGTSTP (suspend), SIGHUP (hangup) — know the common set, not the whole list. Reviewed 2026-08-21: PASS (discriminative — full ladder + why kill -9 skips cleanup; refined: SIGINT/SIGTERM catchable, SIGKILL UNcatchable). Advanced 3d → 7d. Wiki: [[MIT Missing Semester — Command-line Environment]]. |
| Job Control (Ctrl-Z, fg/bg, nohup/disown) | developing | Background Jobs | 2026-08-21 | 2026-08-28 | https://missing.csail.mit.edu/2026/command-line-environment/ | definitional | Ctrl-Z sends SIGTSTP (suspend, not kill); `fg %1`/`bg %1` resume; `jobs` lists. `&` backgrounds from the start, `$!` = last bg PID. Backgrounded jobs are still children of the terminal → closing it sends SIGHUP and kills them; survive with `nohup cmd &` (before) or `disown %1` (after). Reviewed 2026-08-21: PASS (definitional — nailed why disown uses %job-number vs nohup takes the command: nohup is a wrapper before the job exists, disown references an already-existing job-table slot). Advanced 3d → 7d. Wiki: [[MIT Missing Semester — Command-line Environment]]. |
| SSH: Public-Key Auth & Remote Commands | developing | Pipes, Shell Built-ins & Process Isolation | 2026-08-23 | 2026-08-26 | https://missing.csail.mit.edu/2026/command-line-environment/ | definitional | SSH uses public-key crypto: the **public key** is shareable (server uses it to verify you), the **private key** is your secret — NEVER share it (≈ password). `ssh host cmd` runs a command on the remote machine; its stdout streams back. **Quoting decides where the pipe runs:** `ssh host ls | wc -l` runs `ls` remotely + counts locally; `ssh host 'ls | wc -l'` runs the whole pipeline remotely (only the result crosses the wire). **Passphrase (enriched 2026-08-19):** a passphrase encrypts the private key FILE itself; without one the key is plain text, so anyone with filesystem access can copy it and use it to log into your servers (no password needed). Check/validate with `ssh-keygen -y -f /path/to/key`. **Key-based auth handshake (corrected 2026-08-23):** sign/verify challenge–response — (1) client presents public key (in `~/.ssh/authorized_keys`); (2) server sends a *plaintext* random challenge; (3) client **signs** the challenge with its private key, returns the signature; (4) server **verifies** the signature with the public key — access if valid. (Lecture notes' "server encrypts with public key" framing is inaccurate — corrected to match MIT Security lecture.) **Private key never travels the wire.** Wiki: [[SSH — Public-Key Auth & Remote Commands]]. |
| Shell Positional & Special Parameters | developing | What is the Shell | 2026-08-23 | 2026-08-26 | https://missing.csail.mit.edu/2020/shell-tools/ | definitional | Arguments arrive as plain strings; inside a script reach them via special syntax: `$0` = program name, `$1`–`$9` = positional args (10th+ needs braces `${10}`), `$#` = argument count, `$@` = all args as a list. Unquoted `$@` word-splits on spaces — quote it (`"$@"`) to preserve each argument. Wiki: [[Positional & Special Shell Parameters]]. |
| Process Substitution | developing | Shell Redirections & Standard Streams | 2026-08-23 | 2026-08-26 | https://missing.csail.mit.edu/2020/shell-tools/ | definitional | `<(CMD)` runs CMD, sends its output to a temp file/pipe, and substitutes that **file path** (e.g. `/dev/fd/63`) where the text sits — so commands that expect a *file path* (not stdin) can consume command output: `diff <(ls a) <(ls b)`. NOT variable capture (that is command substitution `$(CMD)`). Lecture notes erroneously said `<(CMD)` captures into a variable — flagged. Wiki: [[Process Substitution]]. |
| Shell Config & Dotfiles | developing | What is the Shell, Shell Built-ins & Process Isolation | 2026-08-19 | 2026-08-22 | https://missing.csail.mit.edu/2026/command-line-environment/ | definitional | Hand-typed shell settings don't survive logout — persist them in the shell config file (`~/.bashrc` / `~/.bash_profile`), which the shell reads on startup. `source ~/.bashrc` evaluates the file line by line in the CURRENT shell (no re-login needed). `PS1` renders the left prompt — the classic first dotfile tweak. **Dotfiles** (hidden plain-text config files, names start with `.`): collect them in one version-controlled folder (~/.dotfiles) and `ln -s` them into the standard locations via an install script → portable environment on any new machine. **Plugin frameworks** (Oh My Zsh) can bloat/slow the shell — add one plugin at a time. `command-not-found.com` shows which package provides a command. Wiki: [[MIT Missing Semester — Shell Configuration & Dotfiles]]. |
| File Permissions (ls -l) | developing | ls & File Listing | 2026-08-04 | 2026-08-10 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | The 10-char string from `ls -l`: char 1 = file type (d=directory, -=regular file, l=symlink). Chars 2-4 = Owner rwx, 5-7 = Group rwx, 8-10 = Others rwx. `-` means permission denied. e.g. `drwxr-xr-x` = directory, owner full access, group/others read+execute only. **`chmod +x file`** adds execute permission — required to run scripts via `./script.sh`. Without it, Bash throws "Permission denied". Wiki: [[MIT Missing Semester — Shell]]. |
| Wildcards & Globs | developing | Basic File Tools | 2026-08-11 | 2026-08-25 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `*` matches zero or more chars (e.g. `report*` matches report.pdf). `?` matches exactly one char (e.g. `c?t.png` matches cat.png but not cart.png). `[abc]`/`[0-9]` matches one char from a set. `{a,b}` is brace expansion — generates combinations (e.g. `file_{draft,final}.txt`), does NOT match existing files. Wiki: [[MIT Missing Semester — Shell]]. |
| Bash Quoting | developing | Shell Navigation & Paths | 2026-08-20 | 2026-08-27 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | Reviewed 2026-08-20: held 7d — flipped quoting semantics (thought ANSI-C `$'...'` expands vars; actually DOUBLE quotes `"..."` expand $/$(cmd); single `'...'` = total literal, ANSI-C = escapes only). Three types: Single `'...'` = absolute literal, no expansion of $, !, escapes. Double `"..."` = allows $USER, $(cmd), \ escapes, but protects spaces/wildcards. ANSI-C `$'...'` = processes \n, \t escape sequences, $ and ! treated as literal. Wiki: [[MIT Missing Semester — Shell]]. |
| Parameter Expansion | developing | Shell Loops | 2026-08-18 | 2026-08-21 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | `${var%pattern}` removes matching suffix, `${var#pattern}` removes matching prefix. E.g. `${FILE%.txt}` strips `.txt` extension. `${file##*/}` strips everything before last `/` (basename). `${var:-default}` provides fallback if var is unset. Combined with loops and command substitution for file manipulation. Wiki: [[MIT Missing Semester — Shell]]. |
| Shell Built-ins & Process Isolation | developing | Shell Navigation & Paths | 2026-08-08 | 2026-08-15 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | **Why cd is a built-in:** child processes inherit parent's memory but CANNOT modify it — OS enforces memory isolation. If `cd` ran as external program, it would change its own working directory, then exit — shell's `$PWD` unchanged. **Internal shell state:** `$PWD`, env vars (`PATH`, `USER`), background jobs, shell options (`set`, `shopt`) — all live in shell process RAM. **Built-ins vs external:** `cd`, `export`, variable assignment, `source`, `set` must be built-ins because they mutate shell state. `ls`, `mkdir`, `grep` work as external because they create filesystem side-effects (disk writes, stdout), not shell memory changes. Wiki: [[MIT Missing Semester — Shell]]. |
| Makefile: Targets, Prerequisites & Recipes | developing | None | 2026-08-12 | 2026-08-19 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | A Makefile rule: `target : prerequisites` then indented `recipe`. Target = output file (e.g. monitor) or action (e.g. `clean`). Prerequisites = input files (e.g. main.c). Recipe = shell commands — **every recipe line must start with a tab** (spaces → error). Rule can have no prerequisites but must have a recipe. Non-goal rules only run when invoked explicitly (e.g. `make clean`). Wiki: [[MIT Missing Semester — Shell]]. |
| Make: Timestamp Evaluation | developing | Makefile: Targets, Prerequisites & Recipes | 2026-08-19 | 2026-09-02 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | make compares modification timestamps: if target doesn't exist → rebuild. If any prerequisite is newer than target → rebuild. If all prerequisites older → skip ("up to date"). This is make's core efficiency mechanism. Add Makefile itself to a target's prerequisites to rebuild when the Makefile changes — make never inspects content, only mtimes: even a comment-only save bumps the timestamp and forces recompilation. Wiki: [[MIT Missing Semester — Shell]]. |
| Make: Dependency Tree Resolution | developing | Makefile: Targets, Prerequisites & Recipes | 2026-08-18 | 2026-09-01 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | make reads rules top-down to build a dependency tree in memory, but executes bottom-up (leaves first). If a prerequisite is missing, make recursively finds its rule and builds it first. The first rule is the default goal (what `make` builds when invoked with no arguments). Wiki: [[MIT Missing Semester — Shell]]. |
| Intermediate Object Files (.o) | developing | Make: Timestamp Evaluation | 2026-08-04 | 2026-08-11 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Split compilation into two stages: `gcc -c file.c -o file.o` (compile to object file, skip linking), then `gcc main.o utils.o -o monitor` (link). Only changed .c files get recompiled. Massively speeds up large projects — recompile only what changed. Wiki: [[MIT Missing Semester — Shell]]. |
| Make Variables (CC, CFLAGS) | developing | Makefile: Targets, Prerequisites & Recipes | 2026-08-08 | 2026-08-12 | https://missing.csail.mit.edu/2026/course-shell/ | discriminative | Variables in Makefiles: `CC = gcc`, `CFLAGS = -Wall -Wextra -std=c17 -g`. Referenced in recipes as `$(CC)` and `$(CFLAGS)`. Change once at top instead of editing every recipe line. `CPPFLAGS = -Isrc -Ithird_party` carries include search paths — that's how `#include "meminfo.h"` / `#include "acutest.h"` resolve. Test-target pattern (Lesson 3): executable depends on test file + module under test + headers; `test:` target runs `./tests/test_meminfo` and rebuilds only when those change. Wiki: [[MIT Missing Semester — Shell]]. |
| Clean Targets & .PHONY | developing | Makefile: Targets, Prerequisites & Recipes | 2026-08-08 | 2026-08-15 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | `clean:` target has no prerequisites, recipe is `rm -f monitor`. `.PHONY: clean` tells make that `clean` is NOT a real file — always run its recipe even if a file named clean exists on disk. Without .PHONY, make would skip the recipe if a file named clean exists (thinks it's up to date). Phony targets avoid name conflicts and improve performance. Default goal = first target in Makefile (targets starting with `.` are skipped unless they contain `/`). Wiki: [[MIT Missing Semester — Shell]].
| GCC Compilation Stages | developing | Intermediate Object Files (.o) | 2026-08-20 | 2026-08-27 | GNU Make & GCC Notes | definitional | Reviewed 2026-08-20: held 7d — stage→flag mapping wrong (`-E`=preprocess, `-S`=compile→asm, `-c`=assembler→.o, link=no-flag final to exe; not -r/-e). GCC compiles C through 4 stages: (1) Preprocessing — expands #include, #define, macros (gcc -E). (2) Compilation proper — translates preprocessed C to assembly (gcc -S). (3) Assembly — assembler converts assembly to object file .o (gcc -c). (4) Linking — linker combines all .o files + libraries into executable. Only stages specified are run. Wiki: [[GCC Compilation Stages]]. |
| Git commit message conventions | developing | What is the Shell | 2026-08-04 | 2026-08-11 | https://missing.csail.mit.edu/2026/course-shell/ | definitional | Conventional commits: `<type>(<scope>): <subject>` — type tells what kind of change (feat/fix/docs/refactor/chore), scope narrows context, subject is imperative mood ≤50 chars. Body explains *why*, not what. Footer references issues and breaking changes. Scannable git log. Wiki: [[MIT Missing Semester — Shell]]. |
| Testable Seam | developing | None | 2026-08-19 | 2026-09-02 | Teach C Lesson 2 + Lesson 3 + Gemini tutoring | definitional | Split side-effecting code (reading live OS files) from pure logic (parsing). Test the parser with stable fixture text instead of live values — live values change every millisecond, so asserts on them are flaky (depend on environment state, not code correctness). The seam is the boundary where a controlled input replaces the real environment. Concrete seam (Lesson 3): `read_meminfo(path, out)` opens the file; `parse_meminfo(text, out)` interprets text — "the second is where most bugs live, so most unit tests belong there." Vendor the test framework (curl acutest.h into third_party/) so the test command is reproducible with only a C compiler. Wiki: [[Testable Seam]]. |
| Arrange-Act-Assert (AAA) | developing | None | 2026-08-19 | 2026-09-02 | Bill Wake 3A (xp123) + Teach C Lesson 2 | definitional | Three phases: Arrange (set up object + collaborators), Act (one targeted action/mutator), Assert (claims about observable outcome, rarely global state). Writing order: Assert First / Frame First — ask "suppose it worked, how would I tell?" before setup. Not dogmatic one-assert-per-test: a series of asserts can explore dimensions of one act (size AND max after one insert). Setup/teardown belong to Arrange — no fourth A for unit tests. Multi-act tests usually should be split. If you can't point to the Act, the test checks too much. Wiki: [[Arrange-Act-Assert (AAA)]]. |
| Static Fixtures & Boundary Cases | developing | Testable Seam | 2026-08-18 | 2026-08-29 | Teach C Lesson 2 + Gemini tutoring | discriminative | Fixture = controlled sample data making a moving target predictable. Boundary/adversarial fixtures: malformed input (extra spaces, tabs, missing headers), extreme values (RAM > 32-bit limits), empty files, max-size strings. Fixture assumptions break in production: format drift, missing fields, overflow — the parser assumed one fixture's shape held everywhere. One happy-path fixture proves nothing about production. **Fixture vs smoke tests:** fixtures = deterministic static text with exact asserts (parse("10")==10); smoke tests = live /proc/meminfo reads with range/sanity bounds (memory fluctuates between clock cycles) — a sanity shield. Smoke invariants: `total_kb > 0` guards divide-by-zero downstream; `available_kb <= total_kb` is the true invariant (`<` is brittle since `==` is reachable on fresh/controlled systems). **Exact live asserts are flaky** (enriched 2026-08-18): `/proc/meminfo` is a live kernel interface (values change every µs; even MemTotal can hot-plug in a VM), so `assert(total==16384256)` fails on every other machine. **Swap invariants (enriched 2026-08-18):** `swap_free_kb <= swap_total_kb` (can't have more free than total swap); disabled-swap edge (`SwapTotal: 0`) → guard `swap_total_kb > 0` before `100.0 * swap_used_kb / swap_total_kb` or it divides-by-zero; `unsigned >= 0` is a redundant always-true check. Wiki: [[Static Fixtures & Boundary Cases]]. |
| Red-Green-Refactor | developing | Arrange-Act-Assert (AAA) | 2026-08-18 | 2026-09-01 | Teach C Lesson 2 + Lesson 3 + Gemini tutoring | definitional | Red = test fails for a meaningful reason; seeing it fail FIRST proves (1) the test actually runs your code, (2) the assertion can catch failure — no false positives. Green = minimal code to pass. Refactor = improve design without breaking protected behavior. Skipping Red risks a test that passes even when code is broken (dead smoke detector). Useful even without formal TDD. In terminal: nonzero exit = red, exit 0 = green. Red can be a compile-time failure (Lesson 3): `parse_meminfo` doesn't exist yet — the missing symbol is the Red, and the test doubles as an interface spec (name, signature, semantics) before implementation details distract. Lesson 3 practice: missing-MemAvailable test → red → require both fields in the return → green. Wiki: [[Red-Green-Refactor]]. |
| Black-box vs White-box Testing | developing | None | 2026-08-18 | 2026-09-01 | Gemini tutoring + cstack Part 4 | definitional | Black-box/white-box = HOW (visibility: no internal access vs direct internal calls); unit/integration/E2E = WHAT (scope). Black-box integration via process I/O: IO.popen spawns ./db, pipe insert/select/.exit to stdin, assert exact stdout lines — decoupled from memory layout → refactor internals freely while the CLI contract holds. Testing pyramid: many fast unit tests + few integration tests; integration-only suites are slow and failures are hard to localize. Wiki: [[Black-box vs White-box Testing]]. |
| C String Buffer Boundaries | developing | None | 2026-08-15 | 2026-08-22 | cstack db_tutorial Part 4 | definitional | C strings need buffer space for the null terminator: `char username[COLUMN_USERNAME_SIZE + 1]` — max-length input writes '\0' out of bounds without the +1, corrupting output. `sscanf` overflows on long inputs → replaced by `strtok` + `strlen` bounds checks before copying into structs. Boundary tests (max-size, over-max, 1,401 rows capacity, negative IDs) surface bugs happy-path tests miss. Validate before copying; a clean error beats silent corruption. Wiki: [[C String Buffer Boundaries]]. |
| Sentinel Values vs Presence Flags | developing | None | 2026-08-15 | 2026-08-22 | https://gemini.google.com/app/8870dcd71e2919f5 | discriminative | Reusing a payload value (0) to mean "missing" is ambiguous when 0 is a valid quantity — `(out->total_kb != 0 && out->available_kb != 0)` rejects a legitimate zero read (valid zeroes) and only "catches" absent fields by accident (accidental true negative via `{0}` init). Fix: track structural presence with explicit bool flags (`has_total`, `has_available`), set them when the key matches, and return `(out->has_total && out->has_available) ? 0 : -1`. Decouple structural presence from numeric payload; alternatives: bitmask (`found_mask |= BIT`), field counters, or a sentinel outside the legitimate domain. **Smoke-test continuation (2026-08-15):** `{0}` default makes a missing `MemAvailable` silently pass `0 <= total` — green test on fake data. `ULONG_MAX` sentinel fixes it: `available_kb != ULONG_MAX` in the smoke test proves the key was found. Sentinel is safe only when outside the legitimate domain (ULONG_MAX for memory sizes); presence flags work universally. Three-fact smoke assertion: `read==0 && total>0 && available<=total && available!=ULONG_MAX`. Wiki: [[Sentinel Values vs Presence Flags]]. |
| Feature Probing vs Kernel Version Checking | developing | Sentinel Values vs Presence Flags | 2026-08-18 | 2026-08-25 | https://gemini.google.com/app/e338aa05afbec7a2 | discriminative | Tools like free/top probe feature presence at runtime, never kernel version — version checks are fragile (features backported to older kernels by RHEL/Debian, uname spoofable) and multi-binary shims are a maintenance nightmare. Pattern: sentinel default (ULONG_MAX) → if key still sentinel after parse, feature unsupported → use a documented fallback formula. `MemAvailable` added in Linux 3.14; pre-3.14 estimate ≈ MemFree + Buffers + Cached (fields present since 2.6). Probe capability, not version; degrade gracefully, never crash or lie. **Which field for "available to start an app" (enriched 2026-08-18):** `MemAvailable` — `MemFree` is only *strictly unused* RAM (Linux keeps it low; unused RAM is wasted), `MemAvailable` = kernel estimate of MemFree + reclaimable cache/buffers that can be thrown away without swapping, `SwapFree` is disk/swap space, not RAM. Wiki: [[Feature Probing vs Kernel Version Checking]]. |
| sscanf %n & Line Advancement | developing | None | 2026-08-20 | 2026-08-27 | https://gemini.google.com/app/8870dcd71e2919f5 | discriminative | Reviewed 2026-08-20: pass (whole-line advancement safest; %n partial-match drift). Advanced 3d → 7d. `strchr(line, '\n')` is not line-aware: a missing '\n' in the MIDDLE of a buffer makes it jump to the NEXT newline, silently skipping valid lines; missing '\n' on the last line → NULL → clean break (that line was already parsed). `sscanf` `%n` reports bytes consumed up to that point — but `%lu` stops at the first non-digit, so `line += consumed` leaves `" kB"` behind and the whitespace-skip loop stalls at `'k'`; the next `sscanf` then *succeeds* on the shifted line (key `"kB\nMemAvailable"` — `%[^:]` swallows the newline) and the value is silently lost, the loop dying a line later. Fix: match the full line pattern (`"%31[^:]: %lu kB%n"`) or advance by whole lines (strchr/fgets/strtok_r). sscanf reads tokens, not lines. Wiki: [[sscanf %n & Line Advancement]]. |
| C Pointers (&, *, ->) | developing | None | 2026-08-18 | 2026-08-25 | https://gemini.google.com/app/8870dcd71e2919f5 | discriminative | `&` = address-of ("which box number"); `*` in a declaration = "holds an address", `*` in code = dereference (open the box); `->` is shorthand for `(*ptr).field`. C passes args by value — passing `&memory` to `parse_meminfo(input, &memory)` lets the callee mutate the caller's struct (simulated pass-by-reference). `*p = 20` writes through the pointer: x becomes 20. **Memory lifetime (enriched 2026-08-18):** a pointer is valid only while its region lives; returning a stack array's address (`return text;` of a local `char text[4096]`) → DANGLE on return (UB; GCC/Clang warn). A stack array cannot leak (stack vanishes on return) — only heap leaks when you skip `free()`. `char *buf = malloc(4096);` → `buf` (pointer) on stack, the 4096-block on heap. See [[C Memory Regions (Stack vs Heap vs Swap)]]. Wiki: [[C Pointers (&, *, ->)]]. |
| C Preprocessor Macros | developing | None | 2026-08-19 | 2026-09-02 | https://gemini.google.com/app/8870dcd71e2919f5 | definitional | `#define` = dumb text substitution before compilation (automatic Find & Replace). Object-like (BUFFER_SIZE → 1024) vs function-like (SQUARE(x) → ((x)*(x))). Can capture source metadata (__FILE__/__LINE__) — that's how TEST_CHECK reports file:line. No type checking; side-effect hazard: SQUARE(x++) expands to ((x++)*(x++)) — increments twice. Variadic macros (C99 `...`/`__VA_ARGS__`) power TEST_MSG. Smarter alternatives: inline, constexpr/templates (C++), hygienic AST macros (Rust, Lisp, Elixir, Julia). Wiki: [[C Preprocessor Macros]]. |
| Acutest Unit Testing | developing | None | 2026-08-18 | 2026-08-25 | https://github.com/mity/acutest | definitional | Single-header C test runner (vendor acutest.h; auto-generates main()). TEST_LIST = {name, function} pairs + mandatory {NULL, NULL} sentinel. TEST_CHECK logs failure and continues; TEST_ASSERT aborts the current test — safe because each test runs in its own child process (OS reclaims on abort). TEST_MSG/TEST_DUMP add printf-style diagnostics on failure (variadic, C99+). CLI: --list/-l, --verbose, run-by-name filter, --exclude; exit 0 = pass, 1 = fail. Wiki: [[Acutest Unit Testing]]. |
| C Integer Mechanics (Underflow & Type Promotion) | developing | None | 2026-08-21 | 2026-08-24 | https://gemini.google.com/app/ed55c6cdf10c8c2a | discriminative | `unsigned long` can't hold a negative — when `available_kb > total_kb`, `total_kb - available_kb` underflow-wraps to a huge positive near 2^64 (~18 quintillion kB), so the guard must catch corrupt state BEFORE the subtraction/division (`fputs("invalid memory totals\n", stderr); return 1;`). Percentage trap: `100.0 * used_kb / total_kb` — the `100.0` float literal promotes `used_kb` to `double`; but `(used_kb / total_kb) * 100` does INTEGER division first, truncating the fraction (8M/16M = 0) → 0.0% for any usage <100%. Lead with a float literal so the division stays floating-point. Reviewed 2026-08-21: HELD 3d — knew `100.0` matters + "integers lose accuracy" but INVERTED cause/cure (called promotion the problem, called integer-division truncation a float issue). Core rule: promotion happens before the op; result type follows operands; integer division truncates. Wiki: [[C Integer Mechanics (Underflow & Type Promotion)]]. |
| C Memory Regions (Stack vs Heap vs Swap) | developing | C Pointers (&, *, ->) | 2026-08-18 | 2026-08-22 | https://gemini.google.com/app/ed55c6cdf10c8c2a | definitional | Stack = fast auto scratchpad; a frame per function call, wiped on return (caller's/main stack outlives callee), fixed size (~few MB). Heap = `malloc`/`realloc` warehouse, persists until `free()` (→ memory leaks). Swap = disk overflow when RAM is full, slow. `char *buf = malloc(4096);` — the pointer `buf` lives on the STACK, the 4,096-byte block on the HEAP. Returning a stack array's address → dangling pointer (UB, compiler warning); a stack array CANNOT leak (only heap can). Buffer-passing (`read_file_safe(path, buf, size)`): zero malloc overhead + leak-proof, but fixed capacity truncates `/proc/meminfo` → missing `MemAvailable`. Wiki: [[C Memory Regions (Stack vs Heap vs Swap)]]. |

### Curriculum covered-markers (Stage 0)

> Lessons already covered by live concepts below — marked `done` in `CURRICULUM.md`. When the curriculum reaches a covered lesson, run a **retrieval check** to verify (never re-teach); on failure, demote the lesson back to `in-progress` and correct the concept status.
>
> **A1 First compile (make)** ↔ Makefile: Targets/Prerequisites/Recipes, Make Variables (CC, CFLAGS), Clean Targets & .PHONY, GCC Compilation Stages, Intermediate Object Files
> **A2 First tests (AAA)** ↔ Arrange-Act-Assert (AAA), Red-Green-Refactor
> **A3 Acutest + parser seam** ↔ Acutest Unit Testing, Testable Seam, Static Fixtures & Boundary Cases
> **A4 Read real memory** ↔ Sentinel Values vs Presence Flags, Feature Probing vs Kernel Version Checking, sscanf %n & Line Advancement, C Pointers (&, *, ->), C String Buffer Boundaries

---

## Mastery Summary

- **SWE (Software Engineering Fundamentals):** 52 developing (Shell extras + Makefiles from MIT Missing Semester + Gemini tutoring + Software Testing + C parsing patterns + C pointers/macros/Acutest + feature probing + C integer mechanics + C memory regions + signals & job control + SSH + shell config & dotfiles)
- **Not Started:** 0
- **Paused:** ~87 (archived on 2026-07-22 during roadmap shift)
- **Consolidated:** 0
- **Total concepts tracked:** ~118

---

## Open Questions

> Questions that emerged during sessions but haven't been fully resolved yet. The assistant surfaces these at the start of every session.

- None currently