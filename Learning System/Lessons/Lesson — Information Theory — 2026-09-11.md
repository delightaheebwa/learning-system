# Lesson — Information Theory (Phase 1 L09) — 2026-09-11

Status: **paused mid-Checkpoint 5/6 (2026-09-18)** — CP5 idea + four-form breakdown + KL-connection walkthrough + nonnegativity re-digest (Gibbs/Jensen formalized) delivered and exit-ticketed 3/3; CP5 practice (fresh joint) still pending

## Resume from
Resume **inside CP5** (mutual information), NOT at the idea. 2026-09-18 session (clock rolled past midnight into 2026-09-19 during the exit ticket — same session; label offset surfaced, not merged). Resume chain from 09-16 executed: (1) **nonnegativity re-digest DELIVERED and TESTED** — rebate/cost framing (model overestimation = per-outcome saving / negative term, underestimation = cost / positive term; the truth supplies the weights, so rebates can never pay the costs — Gibbs/Jensen), built on the learner's own "punish/reward" hypothesis which was flipped and corrected (grade-audit agreed FAIL; misconception logged — per-term KL signs ≠ correlation sign); then Gibbs' inequality stated formally: Σ Q_i·log2(Q_i/P_i) ≥ 0, equality iff P = Q; one-line Jensen proof walked (weights Q_i, inputs P_i/Q_i → weighted average collapses to Σ P_i = 1 → log 1 = 0 → negate); coding reductio connected as the information-theoretic reading. Learner follow-ups answered and fact-checked: Jensen-vs-Gibbs division of labor (Johan Jensen 1906, general concave-function tool; Josiah Willard Gibbs 1902 the distribution statement — "Gibbs is the what, Jensen is the how"); "negate both sides" = multiply the inequality by −1 (arithmetic, flips ≤ → ≥; term-wise log(a/b) = −log(b/a)); concavity⇒chord link (curve ≥ chord is the definition; averages live on the chord: function-of-average point on the curve vs average-of-function point on the chord; log2 example 2.32 ≥ 1.585 at x=5 between 1 and 9); the **deliberate numerator reversal** (KL keeps Q on top with Q weights; Jensen is applied to P/Q because ONLY that direction collapses — Q/P gives Σ Q_i²/P_i = 2.778 on W2, no collapse — then term-wise negation). All claims verified on the deepseek-v4.1-flash verifier after the muse-spark verifier went down (503 → 429 → 503; learner directed the re-dispatch). (2) **E1 KL-form repair RETRIEVED — the slip RECURRED**: warm-up W3 asked the form directly and the learner answered "X (the truth) then Y" (sure) — variables instead of distributions (should be joint p(x,y) ∥ product of marginals p(x)p(y)); truth-slot identification (first) was right; grade-audit agreed FAIL; Mistakes row stays active. **Warm-up**: W1 floor-direction MCQ PASS (C, sure) — **this IS the overdue CP3 re-test (due 2026-09-17); passed under sure; re-seal complete**; W2 KL(Q∥P) on paper PASS (0.737 bits exact, sure); justification spot-check on W2 FAIL ("P and Q have a negative relationship at that outcome" — category slip: per-term KL signs over one outcome space vs correlation sign between two variables; anti-correlated Y=1−X has negative correlation yet maximal MI = 1 bit). **Exit ticket (today's material only, quiz-audit PASS after one revision cycle)**: E1 Gibbs' inequality MCQ PASS (C, sure); E2 Jensen-collapse MCQ PASS (A, sure); E3 rebate/cost free recall PASS ("weighted by the truth; truth weights overestimates lightly, underestimates highly", sure). Attempts: Cross-Entropy from NLL pass + KL Divergence pass (warm-up), Mutual Information fail (W3), KL Divergence pass (exit ticket; interval_index 3, next_review 2026-10-19). Resume from: **(1) CP5 practice — fresh computation of I(X;Y) on a NEW joint (still never tested), (2) V(X,Y) = H(X,Y) − I(X,Y) consolidation (wonder-out from 09-16, not consolidated), (3) CP6 (perplexity + bits/nats + label smoothing), (4) Feynman explain-back, then final cumulative quiz and lesson-end handoff.** Convention note (for CP5+): this lesson uses Olah-style H(P,Q) with weights from truth Q, surprises from model P, so the floor is H(Q); Rohit/ML convention flips the letters (P = data → H(P,Q) ≥ H(P)). Anchor phrase: "the entropy term is always the data's entropy". Letter-convention contradiction re-surfaced and re-verified 2026-09-18 (fact-check flagged Rohit en.md P=data vs Olah p=model again).

## Checkpoints
Lettering used throughout this list: **this lesson's Olah letters — model P, truth/data Q**, so every subtracted entropy below is H(data). (Rohit/ML letters flip P and Q; translate before comparing to the `en.md`.)

1. CP1 — Surprise I(x) = −log p — ✅ done (sign fixed: 3 / 0.014 / 0 all correct)
2. CP2 — Entropy H(P) = expected surprise — ✅ done (sign re-sealed on the 2026-09-11 exit ticket; die entropy +2.585 bits)
3. CP3 — Cross-entropy H(P,Q) = CE loss = NLL — ✅ done (2026-09-12: practice 1 bit exact; floor misconception corrected + re-sealed on exit ticket E3; convention question answered, verified). ⚠️ 2026-09-14 resume warm-up W4 REGRESSED the floor DIRECTION (picked H(P,Q) ≤ H(Q), tag sure; floor identity itself correct on W2) — explanation re-delivered at CP4's opening 09-14. ✅ RETRIEVAL RE-TEST PASSED 2026-09-18 (resume warm-up W1: picked H(P,Q) ≥ H(Q), sure; grade-audit agreed) — the overdue 09-17 re-test is done; Mistakes row can graduate (Clerk).
4. CP4 — KL = CE − H(data), i.e. KL(Q∥P) = H(P,Q) − H(Q) in this lesson's letters (not H(P) — H(P) is the model's own entropy) — ✅ done (2026-09-14: idea fact-checked 5/5; asymmetry clarification fact-checked 4/4; practice P1 a/b/c = 1.000 / 0.469 / 0.531 bits + P2 C all correct+sure; grade-audit PASS; attempts: KL Divergence + Cross-Entropy from NLL pass). ⚠️ 2026-09-15 resume warm-up REGRESSED CP4 (identity plus-error + ∞-case + non-symmetry, all sure) → re-sealed same session: fact-checked re-lay + RS1 on-paper pass (0.278 bits) + exit ticket E1/E2 pass — holds again under test (attempts interval_index 3, next_review 2026-10-15).
5. CP5 — Mutual information I(X;Y) — **in-progress: idea + four-form breakdown (2026-09-15) + KL-connection walkthrough (2026-09-16) + nonnegativity re-digest (2026-09-18) delivered**: nonnegativity now FORMALIZED and TESTED — rebate/cost reading (overestimate = saving/negative term, underestimate = cost/positive term, truth supplies the weights so rebates can never pay the costs), Gibbs' inequality Σ Q_i·log2(Q_i/P_i) ≥ 0 (equality iff P = Q), one-line Jensen proof with the deliberate numerator reversal (P/Q collapses to Σ P = 1 → log 1 = 0; Q/P gives Σ Q²/P — no collapse), Jensen-vs-Gibbs division of labor (Jensen 1906 general concave tool, Gibbs 1902 distribution statement), concavity = curve-above-chords; exit ticket 3/3 (E1 theorem MCQ, E2 collapse-mechanism MCQ, E3 rebate/cost free recall — all sure). **CP5 practice (fresh joint) still pending**, plus the E1 KL-form slip repair (RECURRED 2026-09-18 warm-up W3: "X then Y" — variables not distributions; grade-audit FAIL); V(X,Y) wonder-out to consolidate
6. CP6 — Perplexity + bits/nats + label smoothing — pending

## Sources
Scout digest: context-p1l09-information-theory.json (live-fetched 2026-09-11, no drift)
Rohit en.md + Olah (Visual Information Theory) + PyTorch CrossEntropyLoss + Shannon 1948

## Build language
Python (Rohit `Language: Python`)

## Priority-1 misconception (tracking)
−log sign inversion — recurred 3× (probe Q1, probe Q5 CE-vs-KL, CP2 die entropy). CP1, CP2 re-sealed on the 2026-09-11 exit ticket. Re-tested again 2026-09-12 (warm-up W1 = +3 bits, sure; CP3 practice + exit ticket all sign-correct) — holds across 4 consecutive tests.

## Floor misconception (second tracked, 2026-09-12)
"Floor = model's entropy" — answered 1 bit (model H(P)) instead of H(Q) ≈ 0.811 for Q=(0.75,0.25). Corrected same session; re-sealed on exit ticket E3 (picked H(Q)≈0.47 over near-miss H(P)≈0.81 on a new coin). Mistakes row → review, retries=1.
⚠️ REGRESSED 2026-09-14 resume warm-up: W4 picked the REVERSED inequality H(P,Q) ≤ H(Q) (tag sure) while W2 identified floor=H(Q)≈0.881 correctly — floor's identity known, direction re-inverted. Own Mistakes row dated 2026-09-14 (active, due 2026-09-17). Attempt logged under canonical key Cross-Entropy from NLL (fail, then same-day CP4-practice pass — NOT a floor-direction retrieval re-test; next_review 2026-09-21).
✅ Re-test PASSED 2026-09-18 (resume warm-up W1: H(P,Q) ≥ H(Q) picked, sure; grade-audit agreed) — the direction now holds after explanation AND under retrieval; Clerk can graduate the 2026-09-14 Mistakes row.
